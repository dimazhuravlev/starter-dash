# Mapbox GL JS access token (required for the map and for geocoding script).
# Get a token at https://account.mapbox.com/access-tokens/
VITE_MAPBOX_ACCESS_TOKEN=

# Optional: for npm run geocode (defaults to VITE_MAPBOX_ACCESS_TOKEN if set)
# MAPBOX_TOKEN=
